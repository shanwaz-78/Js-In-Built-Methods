// The Array.isArray() static method determines whether the passed value is an Array.

const checkarr = ["Grapes","Butter Milk","Banana"];
const arrval = Array.isArray(checkarr);
// Expected output "true";
console.log(arrval);