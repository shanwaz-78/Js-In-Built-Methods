// The map() method creates a new array populated with the results of calling a provided function.
//  on every element in the calling array.

const example = [2,4,8,10];
const val = example.map(x => x * 2);

console.log(val)
// Expected output;
// [4,8,16,20];